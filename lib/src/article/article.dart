/// A placeholder class that represents an entity or model.
class Article {
  const Article(this.containerId, this.title, this.url, this.thumbnailUrl);

  final String containerId;
  final String title;
  final String url;
  final String thumbnailUrl;
}
